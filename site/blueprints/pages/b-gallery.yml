title: Gallerij
options:
  changeStatus: true
tabs:
  content:
    sections: # here come the sections
      fields: # a fields section
        type: fields
        fields:
          line1:
            type: line

          img1:
            label: Afbeeldingen
            type: files
            multiple: true
            uploads: image
            query: page.images

            # section1: sections/example

  # files: tabs/files
  bestanden: tabs/files
