title: Realisaties
options:
  changeStatus: true
tabs:
  content:
    sections: # here come the sections
      fields: # a fields section
        type: fields
        fields:
          line1:
            type: line

      pages: sections/builder

      realisaties:
        type: pages
        headline: Realisaties categories
        layout: cards
        image:
          query: page.img1.toFile
          cover: true
        templates:
          - realisatie-cat

        # section1: sections/example

  # files: tabs/files
  bestanden: tabs/files
  seo: seo/page
