title: Home
options:
  changeStatus: true
tabs:
  content:
    sections: # here come the sections
      fields: # a fields section
        type: fields
        fields:
          line1:
            type: line
          
          banner1: components/banner

          blocks1: sections/components

        # section1: sections/example

  # files: tabs/files
  bestanden: tabs/files
  seo: seo/page
