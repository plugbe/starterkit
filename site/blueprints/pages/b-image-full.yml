title: Fullscreen beeld
options:
  changeStatus: true
tabs:
  content:
    sections: # here come the sections
      fields: # a fields section
        type: fields
        fields:
          line1:
            type: line
          
          img1:
            label: Afbeelding
            type: files
            multiple: false
            uploads: image
            query: page.images

            # section1: sections/example

  # files: tabs/files
  bestanden: tabs/files
