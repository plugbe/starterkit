title: Horizontale cards
options:
  changeStatus: true
tabs:
  content:
    sections: # here come the sections
      fields: # a fields section
        type: fields
        fields:
          line1:
            type: line
          
          title1:
            label: Titel
            type: info
            text: |
              Titel is bepaald adhv pagina titel.

      cards:
        type: pages
        headline: Cards
        layout: table
        image: false
        templates:
          - b-card


            # section1: sections/example

  # files: tabs/files
  bestanden: tabs/files
